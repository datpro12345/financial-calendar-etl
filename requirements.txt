# Weekly ABCD, Docker, and tests. Do not add Playwright here —
# historical HTML scrape is optional and not the default path.
pandas>=2.0
beautifulsoup4>=4.12
lxml>=5.0
curl_cffi>=0.7
cloudscraper>=1.2.71
pytest>=8.0
